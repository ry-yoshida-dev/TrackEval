[metadata]
name = trackeval
version = 1.0.dev1

[options]
package_dir =
    = src
packages = find:

[options.packages.find]
where = src

[options.package_data]
trackeval =
    py.typed
    datasets/burst_helpers/*.json
